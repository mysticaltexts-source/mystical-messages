{
  "buildCommand": "npm run build && mv dist/index.html dist/app.html && cp dist/landing.html dist/index.html",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/app.html" }
  ]
}

